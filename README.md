# Grid
const graphData = { /* paste the entire JSON above here */ }; Graph3D().graphData(graphData).nodeLabel('name').nodeColor('color').nodeVal('size').linkColor('color').linkDirectionalArrowLength(6).linkDirectionalArrowRelPos(1).linkWidth(2);
